import { SafeAreaView, Text } from "react-native";
interface IProps {}

const SignUp: React.FC<IProps> = () => {
  return (
    <SafeAreaView>
      <Text>SignUp</Text>
    </SafeAreaView>
  );
};

export default SignUp;
