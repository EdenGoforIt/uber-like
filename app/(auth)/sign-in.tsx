import { SafeAreaView, Text } from "react-native";
interface IProps {}

const SignIn: React.FC<IProps> = () => {
  return (
    <SafeAreaView>
      <Text>SignIn</Text>
    </SafeAreaView>
  );
};

export default SignIn;
